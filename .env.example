PORT=5000
NODE_ENV=development

DATABASE_URL=postgresql://USER:PASSWORD@HOST:PORT/DATABASE
DIRECT_URL=postgresql://USER:PASSWORD@HOST:PORT/DATABASE

SHADOW_DATABASE_URL="postgresql://prisma:PASSWORD@localhost:5433/prisma_shadow?schema=public"

TEST_DATABASE_URL="postgresql://prisma:PASSWORD@localhost:5433/wallet_test?schema=public"

JWT_ACCESS_SECRET=replace_with_secure_access_secret
JWT_REFRESH_SECRET=replace_with_secure_refresh_secret

# Development/staging only: fixed OTP for local and staging testing.
# Ignored when NODE_ENV=production; production must use secure random OTPs
# delivered through the real SMS/WhatsApp provider integration.
DEV_FIXED_OTP=000000

RATE_LIMIT_MAX=100
RATE_LIMIT_WINDOW_MS=900000
